/**
 * 类型补充定义
 * UserInfo 需要包含 token 字段
 * 此文件为临时补充，待 src/types/index.ts 可修改时合并
 */

export {};

// 扩展全局 UserInfo 类型
declare module '@/types' {
  // 注意：实际 src/types/index.ts 中的 UserInfo 需要增加 token 字段
}
